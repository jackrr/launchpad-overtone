(ns launchpad-overtone.modes.looper
  (:require [launchpad-overtone.pad :as pad]))

;; TODO:
;; time/metronome visual
;; recording
;; - schedule vs immediate recording start
;; - visual feedback of recording modes
;; play/pause/stop playback
;; loop length programming
;; record over vs playback
;; multiple registers on pad
